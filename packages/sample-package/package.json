{
  "name": "@monorepo-skeleton/sample-package",
  "version": "1.0.0",
  "description": "this is a sample package in the repo",
  "main": "dist/index.js",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.14.35",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "clean": "rm -rf dist/",
    "compile": "tsc -p tsconfig.json",
    "build": "yarn clean && yarn compile",
    "start": "node dist/index.js"
  }
}
